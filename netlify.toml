[build]
command = "pnpm install && NODE_OPTIONS='--max-old-space-size=8192' pnpm generate"
publish = "dist"

[build.environment]
COREPACK_INTEGRITY_KEYS = "0"
NODE_VERSION = "24"
NODE_OPTIONS = "--max-old-space-size=8192"

# Redirect rules live in public/_redirects (netlify.toml redirects were not being
# included in deploys, see git history)
